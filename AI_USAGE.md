# AI 使用说明

## 使用方式

本作业使用 GitHub Copilot CLI 辅助完成需求提取、方案比较、代码实现、自审和测试。AI 直接读取了题目 PDF；当前代码、测试和文档的大部分初稿由 AI 生成，提交者负责最终审阅、运行和对工程决策承担责任。

本次对话的主要步骤：

1. 要求 AI 阅读目录中的作业并提出方案。
2. 要求 AI 对自己的第一版方案进行审查。
3. 在修正系统边界和可靠性表述后，批准开始实现。
4. AI 编写代码，并通过格式化、单元测试和静态检查迭代修正。

当前可用的两个专用 Skill 分别用于 Copilot cloud agent 环境配置和 GitHub PR 媒体上传，与本题无直接关系，因此未使用。

## AI 提供帮助的关键位置

### 1. 从题目中拆解工程问题

AI 将“可靠调用外部 API”拆为：

- 持久化接收；
- 异步领取；
- 幂等和重复窗口；
- 超时、错误分类与重试；
- Worker 崩溃恢复；
- 目标配置和 Secret；
- 死信运营；
- 上游业务事务边界。

这避免了只实现一个带重试的同步 HTTP Proxy。

### 2. 可靠性语义分析

AI 识别出 generic HTTP 无法保证 exactly-once，并说明了以下不确定窗口：

```text
供应商已成功处理，但 Worker 在本地记录成功前崩溃。
```

因此最终选择 at-least-once，并把同一个业务幂等键转发给供应商。

### 3. PostgreSQL 队列设计

AI 提供了以下实现细节：

- `UNIQUE(caller_id, idempotency_key)`；
- `FOR UPDATE SKIP LOCKED`；
- `lease_owner`、`lease_token`、`lease_until`；
- attempt 审计记录；
- 目标配置版本快照；
- 最终尝试期间崩溃后的死信处理。

### 4. HTTP 安全与失败分类

AI 补充了：

- 不接受调用方 URL；
- 禁止 Redirect；
- DNS 解析后阻止私网和保留地址；
- 限制 Body；
- 不记录供应商响应 Body；
- 将网络错误、超时、`408`、`425`、`429`、`5xx` 作为可重试失败；
- 将其他 `3xx`/`4xx` 和配置错误视为永久失败。

### 5. 实现与验证

AI 生成了 Go API、Worker、数据库迁移、Docker Compose 演示、单元测试和可选 PostgreSQL 集成测试。编译和测试暴露的问题被继续修正，例如：

- PostgreSQL interval 参数改为毫秒数，避免依赖字符串解析；
- attempt 完成必须确认实际更新了一行；
- API 内部错误增加结构化日志；
- `5xx` 范围从“所有大于 500”修正为 `500-599`；
- Idempotency-Key 限制为可安全转发的可见 ASCII。

## AI 给出或讨论但没有采纳的方案

### Kafka/RabbitMQ/SQS 作为第一版核心

未采纳。第一版仍需要数据库保存幂等、状态和审计；再加入 Broker 会引入数据库与消息的双写/outbox、消息对账和额外运维。当前流量未知，PostgreSQL 已足够验证需求。

### exactly-once 投递

未采纳。AI 自审后明确修正了第一版中可能造成误解的“可靠送达”表述。通用 HTTP 无法在远端处理和本地提交之间建立原子事务。

### 任意 URL、动态 Header 和模板引擎

未采纳。它们会扩大 SSRF、安全、幂等和业务耦合范围。MVP 只接受注册目标、静态配置和不透明 Body。

### 管理 UI、自动回放 API和完整告警平台

未采纳。它们对运营有价值，但不是验证可靠投递核心模型的必要条件。MVP 使用受控 SQL 回放和结构化日志，并记录未来演进方向。

### 全局排序和多区域

未采纳。没有明确需求时，这些设计会引入分区、阻塞、跨区域一致性和故障切换复杂度。MVP 明确不保证顺序。

## 经审阅后确认的关键决策

第一版 AI 方案被要求再次自审。自审发现并修正了以下不足：

- Go 的选择需要说明理由，而不能被当作题目要求；
- “可靠”必须限定为持久化处理和至少一次尝试，而不是永久保证送达；
- Body 应明确为最终序列化的不透明数据；
- 必须说明重试会造成乱序；
- 排队任务应保存目标版本快照；
- 上游业务事务原子性必须由 transactional outbox 解决；
- 第一版不应加入过多管理和基础设施组件。

随后开始实现，意味着这些边界成为当前版本的约束。最终取舍依据是：

1. 先覆盖最主要的丢失和恢复风险；
2. 明确无法提供的保证；
3. 不在流量和业务约束未知时提前引入 Broker、排序、多区域和工作流引擎；
4. 保留可演进接口，例如 Secret Provider、版本化目标和独立 Worker。

## 对 AI 输出的保留意见

AI 能快速列出大量“生产级”能力，但容易把合理的未来能力一次性放入 MVP。使用 AI 时最重要的人工工作不是接受更多功能，而是持续追问：

- 这个能力是否解决题目当前的核心风险？
- 它是否引入新的状态、故障模式或运维组件？
- 它的保证是否真的能由当前系统边界实现？
- 如果暂时不做，是否可以明确记录并安全演进？

提交前仍应由提交者亲自运行演示、阅读 SQL 和故障语义，并确保能够解释每一个取舍。
